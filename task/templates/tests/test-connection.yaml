apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "task.fullname" . }}-test-connection"
  labels:
    {{- include "task.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "task.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
